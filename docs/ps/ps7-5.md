# PS7.5 — UI facelift: from stock Streamlit to a product (35B lane, solo)

The drop-flow and Results pages work but look default. This PS gives
TempoGraph a visual identity for the demo GIF and open-source debut.
**Hard rule: zero functional changes.** Every widget keeps its key, every
tab its position, every session-state contract intact — the 206-test
suite (heavy AppTest coverage) must pass untouched. This is paint, not
plumbing.

## Scope fence

- **Files you may create:** `.streamlit/config.toml`, `ui/theme.py`
- **Files you may modify:** `ui/app.py`, `ui/pages/Results.py`
- Touch NOTHING else. Do not rename or re-key any widget. Do not add
  dependencies.

## Forbidden

`sudo`, `systemctl`, `pip install`, git push. Interpreter:
`/home/ashie/anaconda3/bin/python3`.

## Design direction (follow exactly)

Identity: **a local video-intelligence instrument** — dark-first,
precise, calm. Not a SaaS landing page.

1. **`.streamlit/config.toml`** theme block:
   - base = "dark"; backgroundColor `#14181D`; secondaryBackgroundColor
     `#1C2229`; textColor `#E6EAEE`; primaryColor `#3FBFB5`
     (scrubber teal); font = "sans serif".
2. **`ui/theme.py`** — one function `apply_theme()` both pages call
   first. It injects a single `st.markdown(<style>, unsafe_allow_html)`
   block:
   - Wordmark header: "TEMPOGRAPH" in spaced small caps with a thin
     teal underline rule, small tagline "local video intelligence"
     (replaces the plain st.title on both pages — keep the same heading
     text content available to tests, so keep an `st.title` call but
     style it).
   - Buttons: subtle border, teal hover; primary buttons filled teal.
   - Metrics: card look (surface bg `#1C2229`, 1px `#2A323B` border,
     6px radius, padded).
   - Tabs: remove the boxy default; underline-style active tab in teal.
   - `st.file_uploader` drop zone: dashed 2px `#3FBFB5` border,
     generous padding, centered "Drop a video" hint.
   - Hide Streamlit chrome: `#MainMenu`, `footer` → visibility hidden.
   - Respect `prefers-reduced-motion`: no keyframe animations at all —
     transitions ≤ 150ms only.
3. **`ui/app.py`** landing: center the drop zone in a hero block with
   the wordmark above it; recent-runs cards get the metric-card
   treatment. Progress screen: stage checklist lines get monospace
   stage names and colored status glyphs (queued grey, running teal,
   done green, error red) via small inline markdown — same dict, same
   statuses, styled rendering only.
4. **`ui/pages/Results.py`**: call `apply_theme()`; add the wordmark;
   no other structural edits.

## ACCEPTANCE (paste output)

```bash
/home/ashie/anaconda3/bin/python3 -m pytest -q          # 206 passed, no skips added
timeout 12 /home/ashie/anaconda3/bin/python3 -m streamlit run ui/app.py \
  --server.headless true --server.port 8599 & sleep 8; curl -sf localhost:8599 >/dev/null && echo UI_BOOTS
/home/ashie/anaconda3/bin/python3 -m black ui/theme.py ui/app.py ui/pages/Results.py
git status --porcelain    # only the 4 scoped files
```

(do not commit — gate reviewer commits)
